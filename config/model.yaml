llm:
  model: "deepseek-chat"
  base_url: "https://api.deepseek.com/v1"
  temperature: 0.0
  max_tokens: 4096

database:
  path: "data/conversations.db"

soul:
  path: "config/soul.md"

config_history_dir: "data/config_history"

# 管理员 ID 列表，允许配置多个，无硬编码
admin:
  user_ids:
    - "ou_admin_open_id_here"
